#! /usr/bin/env sh

python_cmd=$(command -v python3 || command -v python || printf '')

if [ -n "${python_cmd}" ]; then
	USER_BASE_PATH=$(${python_cmd} -m site --user-base || printf '')
	export PATH="${USER_BASE_PATH}/bin:${PATH}"
fi
