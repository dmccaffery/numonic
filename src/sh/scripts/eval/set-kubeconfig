#! /usr/bin/env sh

KUBECONFIG="${HOME}/.kube/config"

if [ ! -f "${KUBECONFIG:-}" ]; then
	return
fi

for config in "${HOME}"/.kube/*.config; do

	if [ ! -f "${config:-}" ]; then
		continue
	fi

	KUBECONFIG="${KUBECONFIG}:${config}"
done

export KUBECONFIG
